

#  这里展现运行流程，需要嵌各自的算法中去使用。

if __name__ == "__main__":
    
    
    # 初始化 evader
    # evader = Evader(pos=[0, 0])
    
    # 只处理一次环境信息
    # evader.get_env_info(case:str)
    # evader.update_env(pursuers:List[Pursuer])
    
    
    # 每跑一次loop: 运行一次update env 以及 move一次
    # evader.local_info_and_collision_check(pursuers) 
    # evader.update_env(pursuers)
    # evader.update_memory()
    # evader.select_goal()
    # evader.sel_stgy()
    # evader.move()

    
    pass
    