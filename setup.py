from setuptools import setup, find_packages

setup(
    name="evader",
    version="0.1.0",
    description="A reusable Python package for evader policy",
    author="Junfeng Chen",
    packages=find_packages(),
    install_requires=[],  # 可以写入依赖
    python_requires=">=3.8",
)
